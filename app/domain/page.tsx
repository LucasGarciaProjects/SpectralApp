/**
 * Domain Configuration Page
 * 
 * This page renders the domain validation view for configuring
 * the spectral domain parameters (wavelength range) for analysis.
 */

"use client"

import { WorkflowView } from "@/components/workflow-view"

export default function DomainPage() {
  return <WorkflowView />
}